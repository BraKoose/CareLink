package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// --- 1. Classic CareLink (Teal & Sage) ---
val TealPrimary = Color(0xFF009688)     // Lively happy bright teal
val SageSecondary = Color(0xFFF26419)    // Playful, cheerful energetic orange
val CoolTertiary = Color(0xFFF6BD60)     // Friendly sunny sunflower yellow

// Light Scheme Comfort Colors
val LightBackground = Color(0xFFFFFDF9)  // Warm sunshine creamy off-white background
val LightSurface = Color(0xFFFFFFFF)     // Pure white container
val LightOnBackground = Color(0xFF2B2D42) // Deep navy for high-contrast friendly text
val LightOnSurface = Color(0xFF2B2D42)

// Dark Scheme Comfort Colors
val DarkBackground = Color(0xFF0F172A)   // Soothing night-mode deep slate
val DarkSurface = Color(0xFF1E293B)      // Slightly lighter slate container
val DarkOnBackground = Color(0xFFF1F5F9)  // Soft white for text
val DarkOnSurface = Color(0xFFF1F5F9)
val DarkPrimary = Color(0xFF38BDF8)      // Bright soft cyan accent
val DarkSecondary = Color(0xFF34D399)    // Bright soft mint accent

// --- 2. Pink Orchid Theme (Menstrual Tracker Cover) ---
val PinkPrimary = Color(0xFFD81B60)       // Warm, reassuring deep pink
val PinkSecondary = Color(0xFFEC407A)     // Soft rose petal pink
val PinkBackground = Color(0xFFFFF1F6)    // Comforting lavender blush background
val PinkSurface = Color(0xFFFFFFFF)
val PinkOnBackground = Color(0xFF3F001C)  // Dark deep plum for high contrast

// --- 3. Emerald Health Theme (Fresh Wellness) ---
val EmeraldPrimary = Color(0xFF0F9D58)    // Bright clean leaf green
val EmeraldSecondary = Color(0xFF2E7D32)  // Rich reassuring forest green
val EmeraldBackground = Color(0xFFF1F8F5) // Soft light mint background
val EmeraldSurface = Color(0xFFFFFFFF)
val EmeraldOnBackground = Color(0xFF0B291A)

// --- 4. Warm Sunset Theme (Energizing Peach) ---
val SunsetPrimary = Color(0xFFE65100)     // Rich energetic warm orange
val SunsetSecondary = Color(0xFFFFA726)   // Comforting golden peach
val SunsetBackground = Color(0xFFFFF3E0)  // Creamy soft orange background
val SunsetSurface = Color(0xFFFFFFFF)
val SunsetOnBackground = Color(0xFF3E2723) // Deep espresso brown
